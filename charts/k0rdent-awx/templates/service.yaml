apiVersion: v1
kind: Service
metadata:
  name: {{ include "k0rdent-awx.fullname" . }}
  labels:
    {{- include "k0rdent-awx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k0rdent-awx.selectorLabels" . | nindent 4 }}
